[package]
name = "catcpu"
version = "0.5.4"
edition = "2021"
description = "Lightweight native Windows tray cat whose animation follows CPU load"
license = "MIT"
publish = false

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
